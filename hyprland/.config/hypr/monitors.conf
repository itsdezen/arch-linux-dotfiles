# Auto-detect and enable every connected monitor at its preferred mode.
# Replace with explicit `monitor=NAME,MODE,POSITION,SCALE` lines once the
# real hardware's output names are known (`hyprctl monitors`).
monitor=,preferred,auto,1
