# fcitx5-unikey addon config. Telex is already fcitx5-unikey's built-in
# default (UkTelex) — this file just pins it explicitly so the choice is
# visible/reproducible rather than relying on the addon's compiled-in default.
InputMethod=Telex
