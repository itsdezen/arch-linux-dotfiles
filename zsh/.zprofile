# ~/.zprofile — loaded once on login shell startup
# Use for one-time environment setup that only login shells need.
#
# Nothing here yet — no Homebrew shellenv needed (that was macOS-only);
# pacman/paru installs land directly on the default PATH. Kept as a stow
# target for future login-only setup (e.g. a display-server launch guard).
